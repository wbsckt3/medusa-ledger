plugins {
  id 'java'
}

group = 'com.medusa'
version = '0.1.0'

repositories { mavenCentral() }

java {
  sourceCompatibility = JavaVersion.VERSION_11
  targetCompatibility = JavaVersion.VERSION_11
}

// HTTP sidecar client — see README.md and SPEC.md
