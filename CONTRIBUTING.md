# Contributing

1. Open an issue describing the change.
2. Keep Medusa **Core** free of multi-tenant / payment-gateway / hosting code (that belongs in Medusa Cloud).
3. Add tests under `packages/core/test`.
4. Run `node --test packages/core/test/*.test.js`.
